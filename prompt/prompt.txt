一个甘特条的默认高度太高，在没有换行胶囊布局的情况下，应该减小高度，以保证一屏能放下更多的航班。每个航班都有两条基线，基线位于甘特条的底部，去掉前面的圆点，加粗一点点，让"起飞","放行"两个字穿透在基线条的中间。

两条基线的粗细应该一样。中间的文字不要有背景。将项目内所有英文改为中文。当前时间采用真实时间。

当前时间条动态固定在页面从左往右90分钟刻度上，也就是背景动，刻度条固定不动。允许手动滚动屏幕。

当手动左右滚动屏幕的时候，当前时间刻度应该跟随一起滚动，以保证永远是指在正确的时间上。现在它不会滚动。

默认页面永远显示当前时间前90分钟和未来150分钟。

时间有重叠的，时间胶囊要换行，不要重叠遮挡。

页面不是需要限制可显示时间，如果有时间胶囊落在当前页面以外，就延长时间刻度。不要压缩时间胶囊的长度。

基线只有两条，分别叫“起飞”和“放行”，线条穿过文字的时候被截断，不要穿过文字造成阅读困难。

基线尾部的时间和基线居中对齐

时间胶囊太小了，看不清楚文字，更看不清楚时间和“实”、“计”两个字

超时、待处理、警告、提醒，四个状态的胶囊填充色太淡了，看不清楚。可以用高色相的，字体换白色

不要一味的加大时间胶囊，要考虑里面的字体使用和配色，以及是否做形状分隔。/frontend-ui-ux

时间信息不要垂直排列，因为横向空间够，纵向空间要节约

实和计的字还是有点小，数字字体不好看，需要换一个。整个时间胶囊可以有同色的边框线条，纯白色看不清楚。

“实”、“计”两个字看不清楚，可以加一个背景

灰色阴影也看不清楚啊

实用绿色，计用蓝色

实和计，以及后面的时间的字体大小，应该和前面的一样

胶囊太大了 互相重叠了

胶囊更圆润一点，前面加一个小一些的点，这个点对齐计划时间的刻度

基线的起飞、放行两处字，以及尾部的时间，使用和实、计一样的字体和字号，起飞、放行增加圆角背景

基线尾部的时间加个背景 这样看不清楚

胶囊和胶囊之间：8px，胶囊和基线之间：10px，起飞和放行之间：8px，放行和底部线条：8px

把图例放大一点，放在页面右侧。

左侧的航班信息区域，航班号应该是这个区域中字体最大的最主要信息，放在第一排靠左。航班类型（正班、调机这种）放在第一排靠右，字号和航班号一致。第二排靠左放航班属性（前起、到达、入位这些（换成中文）），第二排靠右放机位、登机口，用不同的底色区别。第三排放COBT时间。注意不要超过最小高度92px，保持在最小高度92px内。

cobt时间标题和时间挨着，不要分开

标题和数字字体、字号保持一致

标签用黑色，不要用灰色

COBT的背景阴影缩短，在同一排靠右放一个播放按钮，用于以后接入视频监控播放

甘特条内，过去时间加一个填充效果，用于和未来时间视觉上的区分

航班和航班之间的分隔不明显

左边的航班信息给一个背景，边框的右侧使用圆角

航班信息栏的背景，用航司的二字码作为底图，字号巨大，只能隐约的看到航司二字码即可


航司二字码字体使用斜体。应该从左至右几乎沾满整个信息框，而不是堆积在右下角

信息框里有背景的元素，在航司二字码上看起来边框很生硬，怎么解决？