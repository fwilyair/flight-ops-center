# Flight Ops Center (FOC) - Frontend

这是一个基于 React + Vite + Tailwind CSS 构建的**航班运行控制中心 (FOC) 甘特图**前端项目。

主要用于可视化展示航班进出港、机位占用、保障节点（如登机、关舱、推出、起飞等）的实时状态，提供高效的调度指挥视图。

## ✨ 主要功能

- **航班甘特图视图**：
  - 基于时间轴的航班条带展示。
  - 支持进港、出港、连班（进+出）等多种航班类型。
  - 自动根据时间轴定位当前时间线。
  - 支持过去时间的“点阵”纹理遮罩。

- **高密度信息卡片**：
  - 左侧固定列展示航班号、属性（正班/货班等）、机位/登机口、COBT 等关键信息。
  - 采用“胶囊”式设计，融合状态与数值，视觉紧凑且清晰。
  - 支持固定宽度对齐，确保多行数据整洁划一。

- **视觉与交互**：
  - **Premium Header**：带有流体渐变动画（Morandi/Vivid Blue）的顶部导航栏。
  - **深色模式**：支持一键切换亮色/深色主题。
  - **微交互**：悬停高亮、阴影浮起、平滑过渡动画。
  - **自适应布局**：响应式设计，适配不同屏幕宽度。

## 🛠 技术栈

- **核心框架**: [React 19](https://react.dev/)
- **构建工具**: [Vite](https://vitejs.dev/)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **样式**: [Tailwind CSS](https://tailwindcss.com/)
- **图标**: Material Symbols Outlined

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```
启动后访问 `http://localhost:5173` 即可查看。

### 构建生产版本

```bash
npm run build
```

## 📂 项目结构

```
flight-ops-center/
├── src/
│   ├── components/       # UI 组件
│   │   ├── GanttRow.tsx  # 甘特图行组件（核心）
│   │   └── Header.tsx    # 顶部导航栏
│   ├── data.ts           # 模拟数据
│   ├── types.ts          # TypeScript 类型定义
│   ├── utils.ts          # 工具函数（时间转换、颜色计算等）
│   ├── App.tsx           # 主应用入口
│   └── index.css         # 全局样式 & Tailwind 指令
├── public/               # 静态资源
└── package.json          # 项目配置与依赖
```

## 🎨 设计规范

- **胶囊样式**: 用于展示状态和数值，统一使用 `rounded-full` 圆角。
- **字体**:
  - 航班号使用 `Consolas` / `Monospace` 等宽字体，配合 `tabular-nums` 确保对齐。
  - 关键数值（时间、机位）使用醒目的字体颜色区分状态（红/绿/蓝）。
- **配色**:
  - 进港/入位/到达: Emerald Green
  - 出港/起飞/正常: Blue / Indigo
  - 延误/异常: Red / Rose
  - 警告/临期: Orange / Yellow

## 📝 版本历史

- **v0.1.0** (Current):
  - 完成甘特图核心视图。
  - 优化左侧信息栏布局，统一胶囊样式与对齐。
  - 实现顶部流体渐变 Header。
  - 适配深色模式。

---
*Generated by Antigravity Agent*
